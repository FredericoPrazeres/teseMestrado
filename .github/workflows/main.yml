name: Microservices CI/CD Pipeline

on:
  push:
    branches:
      - waitTest

jobs:
  wait1:
    runs-on: self-hosted
    steps:
      - name: Wait Step 1
        uses: actions/checkout@v4

      - name: Step 1
        run: echo "Step 1 — waiting..." && sleep 3

      - name: Step 2
        run: echo "Step 2 — waiting..." && sleep 3

      - name: Step 3
        run: echo "Step 3 — waiting..." && sleep 3

      - name: Step 4
        run: echo "Step 4 — waiting..." && sleep 3

      - name: Step 5
        run: echo "Step 5 — waiting..." && sleep 3

      - name: Step 6
        run: echo "Step 6 — waiting..." && sleep 3

      - name: Step 7
        run: echo "Step 7 — waiting..." && sleep 3

      - name: Step 8
        run: echo "Step 8 — waiting..." && sleep 3

      - name: Step 9
        run: echo "Step 9 — waiting..." && sleep 3

      - name: Step 10
        run: echo "Step 10 — waiting..." && sleep 3

      - name: Step 11
        run: echo "Step 11 — done!"

  wait2:
    runs-on: self-hosted
    needs: wait1
    steps:
      - name: Step 1
        run: echo "Wait Step 1 — waiting..." && sleep 3

      - name: Step 2
        run: echo "Wait Step 2 — waiting..." && sleep 3

      - name: Step 3
        run: echo "Wait Step 3 — waiting..." && sleep 3

      - name: Step 4
        run: echo "Wait Step 4 — waiting..." && sleep 3

      - name: Step 5
        run: echo "Wait Step 5 — waiting..." && sleep 3

      - name: Step 6
        run: echo "Wait Step 6 — waiting..." && sleep 3

      - name: Step 7
        run: echo "Wait Step 7 — waiting..." && sleep 3

      - name: Step 8
        run: echo "Wait Step 8 — waiting..." && sleep 3

      - name: Step 9
        run: echo "Wait Step 9 — waiting..." && sleep 3

      - name: Step 10
        run: echo "Wait Step 10 — waiting..." && sleep 3

      - name: Step 11
        run: echo "Wait Step 11 — done!"

  wait3:
    runs-on: self-hosted
    needs: wait2
    steps:
      - name: Step 1
        run: echo "Wait Step 1 — waiting..." && sleep 3

      - name: Step 2
        run: echo "Wait Step 2 — waiting..." && sleep 3

      - name: Step 3
        run: echo "Wait Step 3 — waiting..." && sleep 3

      - name: Step 4
        run: echo "Wait Step 4 — waiting..." && sleep 3

      - name: Step 5
        run: echo "Wait Step 5 — waiting..." && sleep 3

      - name: Step 6
        run: echo "Wait Step 6 — waiting..." && sleep 3

      - name: Step 7
        run: echo "Wait Step 7 — waiting..." && sleep 3

      - name: Step 8
        run: echo "Wait Step 8 — waiting..." && sleep 3

      - name: Step 9
        run: echo "Wait Step 9 — waiting..." && sleep 3

      - name: Step 10
        run: echo "Wait Step 10 — waiting..." && sleep 3

      - name: Step 11
        run: echo "Wait Step 11 — done!"

  wait4:
    runs-on: self-hosted
    needs: wait3
    steps:
      - name: Step 1
        run: echo "Wait Step 1 — waiting..." && sleep 3

      - name: Step 2
        run: echo "Wait Step 2 — waiting..." && sleep 3

      - name: Step 3
        run: echo "Wait Step 3 — waiting..." && sleep 3

      - name: Step 4
        run: echo "Wait Step 4 — waiting..." && sleep 3

      - name: Step 5
        run: echo "Wait Step 5 — waiting..." && sleep 3

      - name: Step 6
        run: echo "Wait Step 6 — waiting..." && sleep 3

      - name: Step 7
        run: echo "Wait Step 7 — waiting..." && sleep 3

      - name: Step 8
        run: echo "Wait Step 8 — waiting..." && sleep 3

      - name: Step 9
        run: echo "Wait Step 9 — waiting..." && sleep 3

      - name: Step 10
        run: echo "Wait Step 10 — waiting..." && sleep 3

      - name: Step 15
        run: echo "Wait Step 15 — done!"