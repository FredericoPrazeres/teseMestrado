name: Microservices CI/CD Pipeline

on:
  push:
    branches:
      - waitTest

jobs:
  job1:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Step 1
        run: echo "Job 1 - Step 1 — waiting..." && sleep 3

      - name: Step 2
        run: echo "Job 1 - Step 2 — waiting..." && sleep 3

      - name: Step 3
        run: echo "Job 1 - Step 3 — waiting..." && sleep 3

      - name: Step 4
        run: echo "Job 1 - Step 4 — waiting..." && sleep 3

      - name: Step 5
        run: echo "Job 1 - Step 5 — waiting..." && sleep 3

      - name: Step 6
        run: echo "Job 1 - Step 6 — waiting..." && sleep 3

      - name: Step 7
        run: echo "Job 1 - Step 7 — waiting..." && sleep 3

      - name: Step 8
        run: echo "Job 1 - Step 8 — waiting..." && sleep 3

      - name: Step 9
        run: echo "Job 1 - Step 9 — waiting..." && sleep 3

      - name: Step 10
        run: echo "Job 1 - Step 10 — done!" && sleep 3
        