pipeline {
    agent any

    environment {
        PATH = 'C:\\Program Files\\Git\\cmd;C:\\nvm4w\\nodejs;C:\\ProgramData\\npm;${PATH}'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
                bat 'git submodule update --init --recursive'
            }
        }

        stage('Npm install') {
            steps {
                bat 'npm install -g pnpm@10 --force'
            }
        }

        stage('Npm list') {
            steps {
                bat 'npm list -g pnpm'
            }
        }

        stage('Install pnpm') {
            steps {
                dir('directus') {
                    bat 'pnpm install --frozen-lockfile'
                }
            }
        }

        stage('Lint') {
            steps {
                dir('directus') {
                    bat 'pnpm lint'
                }
            }
        }

        stage('Build') {
            steps {
                dir('directus') {
                    bat 'pnpm build'
                }
            }
        }
    }
}



