pipeline {
    agent any

    stages {
        stage('Job 1') {
            steps {
                sh 'echo "Job 1 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 1 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 1 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 1 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 1 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 2') {
            steps {
                sh 'echo "Job 2 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 2 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 2 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 2 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 2 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 3') {
            steps {
                sh 'echo "Job 3 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 3 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 3 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 3 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 3 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 4') {
            steps {
                sh 'echo "Job 4 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 4 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 4 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 4 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 4 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 5') {
            steps {
                sh 'echo "Job 5 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 5 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 5 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 5 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 5 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 6') {
            steps {
                sh 'echo "Job 6 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 6 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 6 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 6 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 6 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 7') {
            steps {
                sh 'echo "Job 7 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 7 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 7 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 7 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 7 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 8') {
            steps {
                sh 'echo "Job 8 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 8 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 8 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 8 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 8 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 9') {
            steps {
                sh 'echo "Job 9 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 9 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 9 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 9 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 9 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 10') {
            steps {
                sh 'echo "Job 10 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 10 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 10 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 10 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 10 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 11') {
            steps {
                sh 'echo "Job 11 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 11 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 11 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 11 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 11 - Step 5 — done!" && sleep 2'
            }
        }

        stage('Job 12') {
            steps {
                sh 'echo "Job 12 - Step 1 — waiting..." && sleep 2'
                sh 'echo "Job 12 - Step 2 — waiting..." && sleep 2'
                sh 'echo "Job 12 - Step 3 — waiting..." && sleep 2'
                sh 'echo "Job 12 - Step 4 — waiting..." && sleep 2'
                sh 'echo "Job 12 - Step 5 — done!" && sleep 2'
            }
        }
    }
}