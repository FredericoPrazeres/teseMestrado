pipeline {
    agent any

    environment {
        NVM_DIR = '/home/frederico/.nvm'
        COREPACK_ENABLE_STRICT = '0'
        COREPACK_ENABLE_AUTO_PIN = '0'
    }

    stages {
        stage('Build') {
            steps {
                checkout scm
                sh 'git submodule update --init --recursive'

                dir('directus') {
                    sh 'rm -rf node_modules'
                }

                dir('directus') {
                    sh '''
                        export NVM_DIR="$HOME/.nvm"
                        . "$NVM_DIR/nvm.sh"
                        nvm use 22
                        pnpm install --frozen-lockfile
                    '''
                }

                dir('directus') {
                    sh '''
                        export NVM_DIR="$HOME/.nvm"
                        . "$NVM_DIR/nvm.sh"
                        nvm use 22
                        pnpm lint
                    '''
                }

                dir('directus') {
                    sh '''
                        export NVM_DIR="$HOME/.nvm"
                        . "$NVM_DIR/nvm.sh"
                        nvm use 22
                        pnpm build
                    '''
                }
            }
        }
    }
}



