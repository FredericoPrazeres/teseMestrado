pipeline {
    agent any

    stages {
        stage('Job 1') {
            steps {
                sh 'echo "Job 1 - Step 1 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 2 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 3 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 4 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 5 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 6 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 7 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 8 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 9 — waiting..." && sleep 3'
                sh 'echo "Job 1 - Step 10 — done!" && sleep 3'
            }
        }
    }
}