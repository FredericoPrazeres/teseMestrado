pipeline {
    agent any

    environment {
        NODE_HOME = 'C:\\Program Files\\nodejs'
        PATH = "${NODE_HOME};${env.PATH}"
        COREPACK_ENABLE_STRICT = '0'
        COREPACK_ENABLE_AUTO_PIN = '0'
    }

    stages {
        stage('Build and Test') {
            steps {
                checkout scm
                bat 'git submodule update --init --recursive'
                dir('directus') {
                    bat 'node --version'
                    bat 'corepack enable'
                    bat 'corepack prepare pnpm@10 --activate'
                    bat 'corepack pnpm install --frozen-lockfile'
                    bat 'corepack pnpm lint'
                    bat 'corepack pnpm build'
                    bat 'corepack pnpm --recursive --filter "!tests-blackbox" --filter "!tests-e2e" --filter "!e2e" --filter "!app" test'
                }
            }
        }
    }
}

