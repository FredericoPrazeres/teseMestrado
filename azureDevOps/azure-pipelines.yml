trigger:
  branches:
    include:
      - applications/directusss

pool:
  name: TeseMestradoPool

jobs:
  - job: RunPipeline
    displayName: 'Checkout repository'
    steps:
      - checkout: self
        clean: false
        submodules: true
        displayName: 'Checkout repository'

      - task: UseNode@1
        inputs:
          version: '22.x'
        displayName: 'Setup Node.js'

      - task: UseNode@1
        inputs:
          version: '22.x'
        displayName: 'Setup Node.js'
      - script: |
          cd directus && pnpm install --frozen-lockfile
        displayName: 'Install dependencies'

      - task: UseNode@1
        inputs:
          version: '22.x'
        displayName: 'Setup Node.js'
      - script: |
          cd directus && pnpm lint
        displayName: 'Lint'

      - task: UseNode@1
        inputs:
          version: '22.x'
        displayName: 'Setup Node.js'
      - script: |
          cd directus && pnpm build
        displayName: 'Build'
