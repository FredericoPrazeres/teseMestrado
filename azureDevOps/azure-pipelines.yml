trigger:
  branches:
    include:
      - waitTest

pool:
  name: TeseMestradoPool

jobs:
  - job: job1
    displayName: 'Job 1'
    steps:
      - checkout: self
        displayName: 'Checkout code'

      - script: echo "Job 1 - Step 1 — waiting..." && sleep 3
        displayName: 'Step 1'

      - script: echo "Job 1 - Step 2 — waiting..." && sleep 3
        displayName: 'Step 2'

      - script: echo "Job 1 - Step 3 — waiting..." && sleep 3
        displayName: 'Step 3'

      - script: echo "Job 1 - Step 4 — waiting..." && sleep 3
        displayName: 'Step 4'

      - script: echo "Job 1 - Step 5 — waiting..." && sleep 3
        displayName: 'Step 5'

      - script: echo "Job 1 - Step 6 — waiting..." && sleep 3
        displayName: 'Step 6'

      - script: echo "Job 1 - Step 7 — waiting..." && sleep 3
        displayName: 'Step 7'

      - script: echo "Job 1 - Step 8 — waiting..." && sleep 3
        displayName: 'Step 8'

      - script: echo "Job 1 - Step 9 — waiting..." && sleep 3
        displayName: 'Step 9'

      - script: echo "Job 1 - Step 10 — done!" && sleep 3
        displayName: 'Step 10'
        